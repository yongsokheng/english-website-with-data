<div class="row">
  <div class="col-md-12">
    <%= form_for [:admin, @user], url: admin_password_path, remote: true do |f| %>
      <%= render "shared/errors_messages", object: @user %>
      <p><%= f.label :current_password %></p>
      <p><%= f.password_field :current_password, class: "form-control" %></p>
      <p><%= f.label :new_password %></p>
      <p><%= f.password_field :password, class: "form-control" %></p>
      <p><%= f.label :password_confirmation %></p>
      <p><%= f.password_field :password_confirmation, class: "form-control" %></p>
      <hr>
      <%= f.submit t("user.admin.buttons.save"), class: "btn btn-primary pull-right" %>
      <button type="button" class="btn btn-default pull-right btn-close" data-dismiss="modal">
        <%= t "admin.buttons.close" %>
      </button>
    <% end %>
  </div>
</div>
