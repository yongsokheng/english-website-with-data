<!-- Navigation -->
<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
      <%= link_to t("admin.titles.header"), admin_root_path, class: "navbar-brand" %>
  </div>
    <!-- Top Menu Items -->
    <ul class="nav navbar-right top-nav">
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
          <%= image_tag current_user.avatar_url, class: "img-circle", width: "30px" %>
          &nbsp; <%= current_user.name %> <b class="caret"></b>
        </a>
        <ul class="dropdown-menu">
          <li><%= link_to t("admin.menus.profile"), admin_profile_path(current_user) %></li>
          <li class="divider"></li>
          <li>
            <%= link_to t("admin.menus.logout"), destroy_user_session_path, method: :delete %>
          </li>
        </ul>
      </li>
    </ul>
    <!-- Sidebar Menu Items - These collapse to the responsive navigation menu on small screens -->
    <div class="collapse navbar-collapse navbar-ex1-collapse">
      <ul class="nav navbar-nav side-nav">
        <li class="<%= active? "dashboards" %>">
          <%= link_to t("admin.menus.dashboard"), admin_dashboards_path %>
        </li>
        <li class="<%= active? "users" %>">
          <%= link_to t("admin.menus.user"), admin_users_path %>
        </li>
        <li class="<%= active? "categories" %>">
          <%= link_to t("admin.menus.category"), admin_categories_path %>
        </li>
        <li class="<%= active? "articles" %>">
          <%= link_to t("admin.menus.article"), admin_articles_path %>
        </li>
        <li class="<%= active? "trashes" %>">
          <%= link_to t("admin.menus.trash"), admin_trashes_path %>
        </li>
      </ul>
    </div>
    <!-- /.navbar-collapse -->
</nav>
